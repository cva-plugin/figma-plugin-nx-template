// import { findVariablesByCriteria } from "@code/utils";
// import { VariableSetDataSource } from "@code/metadata/dataSource/variables/VariableValues";

// async run() {
//   const modeId = "103:0";

//   const criteria = {
//     // collectionId: "VariableCollectionId:103:213",
//     namePattern: new RegExp('Background')
//   };

//   const vars = findVariablesByCriteria(criteria);

//   const ds = new VariableSetDataSource('default', criteria);

//   console.warn(JSON.stringify(ds.getEntries(), null, 2));
// }
